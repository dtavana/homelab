# Generated by scripts/generate-pangolin-blueprint.rb. Do not edit by hand.
global:
  podSecurityContext:
    fsGroup: 65534
    fsGroupChangePolicy: OnRootMismatch
  podAnnotations:
    checksum/pangolin-blueprint: "a810c979098b59255ed72e0cf499d7ed54e5b6e7e55015003250b88d7ade465d"
newtInstances:
  - name: main-tunnel
    enabled: true
    auth:
      existingSecretName: newt-main-tunnel-auth
    acceptClients: false
    configPersistence:
      enabled: true
      mountPath: "/var/lib/newt"
      fileName: config.json
      type: persistentVolumeClaim
      existingClaim: newt-main-tunnel-config
    blueprintFile: "/etc/newt/blueprint.yaml"
    blueprintData: |-
      public-resources:
        hass-dtavana-dev:
          name: Hass
          protocol: http
          full-domain: hass.dtavana.dev
          auth:
            sso-enabled: false
          targets:
          - hostname: nginx-system-nginx-ingress-nginx-controller.nginx-system.svc.cluster.local
            port: 80
            method: http
        homepage-dtavana-dev:
          name: Homepage
          protocol: http
          full-domain: homepage.dtavana.dev
          auth:
            sso-enabled: true
          targets:
          - hostname: nginx-system-nginx-ingress-nginx-controller.nginx-system.svc.cluster.local
            port: 80
            method: http
        minecraft:
          name: Minecraft
          protocol: tcp
          proxy-port: 25565
          targets:
          - hostname: minecraft-system-minecraft.minecraft-system.svc.cluster.local
            port: 25565
