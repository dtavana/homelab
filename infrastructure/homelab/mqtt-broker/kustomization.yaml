apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true
resources:
  - ../../base/mqtt-broker
configMapGenerator:
  - name: mqtt-broker-values
    namespace: flux-system
    files:
      - values.yaml=./values.yaml
