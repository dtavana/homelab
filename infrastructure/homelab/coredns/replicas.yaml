# yaml-language-server: $schema=none
# Intentionally partial: Flux applies this server-side so it owns only spec.replicas.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coredns
  namespace: kube-system
spec:
  replicas: 2
