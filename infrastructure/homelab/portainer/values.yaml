persistence:
  enabled: true
  storageClass: "longhorn"
  size: "2Gi"
service:
  type: ClusterIP
ingress:
  enabled: true
  ingressClassName: "nginx-internal"
  hosts:
    - host: "portainer.internal.dtavana.dev"
      paths:
        - path: /
          pathType: Prefix
