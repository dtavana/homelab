apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: daily-backup
  namespace: longhorn-system
spec:
  name: "daily-backup"
  cron: "0 4 * * *"
  task: "backup"
  groups:
    - default
  retain: 5
  concurrency: 1
