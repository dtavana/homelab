apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true
resources:
  - ../../base/ha-mcp
  - ./ha-mcp-token.sops.yaml
configMapGenerator:
  - name: ha-mcp-values
    namespace: flux-system
    files:
      - values.yaml=./values.yaml
