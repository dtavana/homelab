apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pihole
  namespace: pihole-system
spec:
  interval: 15m
  chart:
    spec:
      chart: pihole
      version: 2.19.1
      sourceRef:
        kind: HelmRepository
        name: pihole-repo
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: pihole-values