apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # APPS
  - home-assistant-helmrepo.yaml
  - pihole-helmrepo.yaml
  # STORAGE
  - longhorn-helmrepo.yaml
  # MONITORING
  - portainer-helmrepo.yaml
  # NETWORKING
  - nginx-helmrepo.yaml
